server:
  port: 8083
spring:
  application:
    name: security-prifile
  boot:
    admin:
      client:
        url: [ http://localhost:8081/admin-service ]
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    com:
      bytesw: INFO
    springFramework:
      web:
        client:
          RestTemplate: INFO
datasource:
  mysql:
    url: jdbc:mysql://192.168.2.116:3306/security?autoReconect=true
    username: jjgonzalez
    password: Desarrollo.2022
    driverClassName: com.mysql.cj.jdbc.Driver
    jpa:
      dialect: org.hibernate.dialect.MySQL8Dialect
      showSql: true